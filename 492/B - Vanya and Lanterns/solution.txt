n, l = map(int, input().split())lanterns = list(map(int, input().split())) lanterns.sort() max_gap = 0for i in range(1, n):    gap = lanterns[i] - lanterns[i - 1]    if gap > max_gap:        max_gap = gap  radius = max(max_gap / 2, lanterns[0], l - lanterns[-1])  print(f"{radius:.10f}")