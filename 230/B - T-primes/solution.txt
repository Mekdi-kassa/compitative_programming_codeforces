import math def is_prime(n):    """Check if a number is prime"""    if n < 2:        return False    if n == 2:        return True    if n % 2 == 0:        return False        # Check divisibility up to square root    for i in range(3, int(math.sqrt(n)) + 1, 2):        if n % i == 0:            return False    return True def main():    n = int(input())    numbers = list(map(int, input().split()))        for x in numbers:        root = int(math.isqrt(x))  # integer square root                # Check if it's a perfect square and root is prime        if root * root == x and is_prime(root):            print("YES")        else:            print("NO") if __name__ == "__main__":    main()