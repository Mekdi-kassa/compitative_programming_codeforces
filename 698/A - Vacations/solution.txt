n = int(input())arr = list(map(int, input().split()))memo = {} def dp(i, last):    if i >= n:        return 0        if (i, last) in memo:        return memo[(i, last)]        res = float('inf')        if arr[i] == 0:  # Contest is closed        res = 1 + dp(i + 1, "r")  # Must rest        elif arr[i] == 1:  # Contest is open        if last != "c":  # Can do contest            res = min(res, dp(i + 1, "c"))        res = min(res, 1 + dp(i + 1, "r"))  # Or rest        elif arr[i] == 2:  # Gym is open        if last != "g":  # Can do gym            res = min(res, dp(i + 1, "g"))        res = min(res, 1 + dp(i + 1, "r"))  # Or rest        elif arr[i] == 3:  # Both are open        if last != "c":  # Can do contest            res = min(res, dp(i + 1, "c"))        if last != "g":  # Can do gym            res = min(res, dp(i + 1, "g"))        res = min(res, 1 + dp(i + 1, "r"))  # Or rest        memo[(i, last)] = res    return res print(dp(0, ""))