n, k = map(int, input().split())mod = 10**9 + 7 # dp[len][end]dp = [[0] * (n + 1) for _ in range(k + 1)] # length 1 sequencesfor end in range(1, n + 1):    dp[1][end] = 1 # bottom-upfor length in range(1, k):    for end in range(1, n + 1):        if dp[length][end] != 0:            # for all multiples of 'end'            multiple = end            while multiple <= n:                dp[length + 1][multiple] = (dp[length + 1][multiple] + dp[length][end]) % mod                multiple += end # result: sum of all sequences of length kcount = 0for end in range(1, n + 1):    count += dp[k][end]  print(count % mod)